program teste;
var
    x:real;
    c:boolean;
    z:integer;

procedure limpar (y:integer);
var
    z:boolean;
begin
    begin
        c := false

    end
end;

procedure limpare (y:integer);
var x: integer;
begin
end;

begin
  limpar(4);
  z := 20 + 30;
  c := true or false;
  x := 4 + 4 * (4.5 + 2.4);
  if (x > 2) and (x = 1) then x:= 4;
  do
    x:=4
   while(x > 2)

end
.
